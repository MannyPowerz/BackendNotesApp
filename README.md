# BackendNotesApp
A backend Notes App API built with Node.js and Express, featuring JWT authentication, secure user registration/login with hashed passwords, protected routes, and CRUD operations. Designed as a learning project to practice core backend concepts like authentication, middleware, and error handling.
